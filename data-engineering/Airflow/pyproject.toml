[project]
name = "airflow-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "andru98", email = "andrinashrestha2010@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "apache-airflow>=3.3.0",
    "apache-airflow-providers-standard>=1.16.0",
]

[project.scripts]
airflow = "airflow:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
